% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_plot.R
\name{rank_plot}
\alias{rank_plot}
\title{Rank Plot (Log-Log CCDF)}
\usage{
rank_plot(data, fit = NULL, log_scale = TRUE, na.rm = FALSE, ...)
}
\arguments{
\item{data}{A numeric vector of i.i.d. observations.}

\item{fit}{Optional. A list returned by \code{\link{plfit}}, used to overlay
the fitted Pareto line. Must contain \code{alpha_hat} and \code{xmin_hat}.}

\item{log_scale}{Logical. If \code{TRUE} (default), axes are log-transformed
(i.e., \eqn{\log x} vs \eqn{\log \hat{F}^c}).}

\item{na.rm}{Logical. If \code{TRUE}, missing values (\code{NA}) are removed
before analysis. Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{plot}}.}
}
\value{
A \code{data.frame} with columns \code{x} and \code{ccdf},
returned invisibly.
}
\description{
Plots the empirical complementary CDF (CCDF) of the data on a log-log scale.
A power-law distribution appears as a straight line on this plot. If a fitted
\code{plfit()} result is supplied, the theoretical Pareto CCDF is overlaid.
}
\examples{

set.seed(1)
x <- rpareto(800, alpha = 2, xm = 1)
fit <- plfit(x)
rank_plot(x, fit = fit)

}
\references{
Nair, J., Wierman, A., & Zwart, B. (2022). \emph{The Fundamentals of Heavy
Tails: Properties, Emergence, and Estimation}. Cambridge University Press.
(pp. 176-179) \doi{10.1017/9781009053730}
}
